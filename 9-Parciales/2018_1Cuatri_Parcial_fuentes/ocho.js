function mostrar()
{ 
    var respuesta;
    var letra;
    var nùmero;
    var cantidadDePares;
    var cantidadDeCeros;
    var contadorDeNegativos;
    //do {
    while (isNaN(nùmero) || nùmero < 100 || nùmero > -100){

        nùmero = prompt("Ingrese un nùmero")
    nùmero = parseInt(nùmero);

    respuesta = confirm("¿Desea continuar?");}
        
        while (isNaN(letra)){
            letra = prompt("Ingrese una letra");
} //while (respuesta = true)
    if (nùmero 2 % == 0) {

    }else if (nùmero )

}
