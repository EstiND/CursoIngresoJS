function mostrar()
{ 
    var contador = 0;
    var acumuladorSuma; 
    var minNota;
    var promedio;
    var minSexo;
    var contadorVarones;

    while (contador < 5){
        contador ++;

        do {
            var nota = prompt("Ingrese una nota");
            nota = parseInt(nota);
        } while(isNaN(nota) || nota < 0 || nota > 10)             
         do {
             var sexo = prompt("Ingrese su sexo");
             } while (sexo )
    
}
             
}
